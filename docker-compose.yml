services:
  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    depends_on:
      - relay

  relay:
    build:
      context: ./relay
      dockerfile: Dockerfile
    ports:
      - "3001:3001"

volumes:
  n8n_data:
